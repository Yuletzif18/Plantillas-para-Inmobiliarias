name: Deploy Xero to GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'apps/portafolio/Xero/**'
      - '.github/workflows/deploy-xero.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-xero"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'apps/portafolio/Xero/package-lock.json'

      - name: Install dependencies
        working-directory: apps/portafolio/Xero
        run: npm ci

      - name: Build project
        working-directory: apps/portafolio/Xero
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: 'apps/portafolio/Xero/.output/public'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
